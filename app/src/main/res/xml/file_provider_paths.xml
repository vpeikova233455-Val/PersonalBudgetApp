<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Exposes the app's cache directory so the downloaded APK can be passed
         to the system package installer via a content:// URI. -->
    <cache-path name="update_apk" path="." />
</paths>
